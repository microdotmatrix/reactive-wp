import { CMS_NAME, CMS_URL } from '../lib/constants'

export default function Intro() {
  return (
    <section className="flex-col md:flex-row flex items-center md:justify-between absolute z-10">
      <h1 className="tracking-tighter leading-tight md:pr-8">
        Blog.
      </h1>
    </section>
  )
}
